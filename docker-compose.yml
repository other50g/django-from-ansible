version: '3'
services:
  ansible:
    build: ./ansible
    tty: true
    working_dir: '/var/data'
    volumes:
      - ./ansible:/var/data
  target01:
    build: ./target
    tty: true
    privileged: true
    command: /sbin/init
    ports:
      - 8999:80
